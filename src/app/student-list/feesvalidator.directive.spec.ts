import { FeesvalidatorDirective } from './feesvalidator.directive';

describe('FeesvalidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new FeesvalidatorDirective();
    expect(directive).toBeTruthy();
  });
});
