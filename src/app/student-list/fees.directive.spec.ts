import { FeesDirective } from './fees.directive';

describe('FeesDirective', () => {
  it('should create an instance', () => {
    const directive = new FeesDirective();
    expect(directive).toBeTruthy();
  });
});
